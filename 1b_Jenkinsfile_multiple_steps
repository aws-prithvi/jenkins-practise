pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "This is First Jenkins Pipeline for practise"'
                sh '''
                    echo "This is step script"
                    ls -ltr
                '''
            }
        }
    }
}
